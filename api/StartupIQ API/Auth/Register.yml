info:
  name: Register
  type: http

http:
  method: POST
  url: http://localhost:5000/auth/register
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​